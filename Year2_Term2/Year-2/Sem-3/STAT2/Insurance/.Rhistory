model_summary
# Define the hypotheses
H0 <- "The model with the predictors ln(x1), ln(x2), and ln(x3) is not useful in predicting ln(y) (all coefficients are zero)"
HA <- "At least one of the predictors ln(x1), ln(x2), or ln(x3) is useful in predicting ln(y) (at least one coefficient is not zero)"
# Print the hypotheses
cat("Null Hypothesis (H0):", H0, "\n")
cat("Alternative Hypothesis (HA):", HA, "\n")
# Extract the F-statistic and p-value
f_statistic <- model_summary$fstatistic[1]
p_value <- pf(f_statistic, model_summary$fstatistic[2], model_summary$fstatistic[3], lower.tail = FALSE)
# Print the results
cat("F-statistic:", f_statistic, "\n")
cat("p-value:", p_value, "\n")
# Check the significance at 0.05 level
if(p_value < 0.05) {
cat("The model is significant at the 0.05 level.\n")
} else {
cat("The model is not significant at the 0.05 level.\n")
}
# Input the data
x1 <- c(0.93,1.11,0.93,1.11,0.93,1.11,0.93,1.11,1.02,1.02,1.02,1.02)
x2 <- c(1.00,1.00,1.00,1.00,1.40,1.40,1.40,1.40,1.18,1.18,1.18,1.18)
x3 <- c(0.20,0.20,0.50,0.50,0.20,0.20,0.50,0.50,0.31,0.31,0.31,0.31)
y <- c(32.95,38.72,35.20,38.72,32.27,39.71,33.67,38.72,35.20,33.67,36.02,32.27)
# Transform the variables using natural logarithms
ln_x1 <- log(x1)
ln_x2 <- log(x2)
ln_x3 <- log(x3)
ln_y <- log(y)
# Create a data frame
data <- data.frame(ln_x1, ln_x2, ln_x3, ln_y)
# Fit the linear regression model
model <- lm(ln_y ~ ln_x1 + ln_x2 + ln_x3, data = data)
# Display the summary of the model
model_summary <- summary(model)
model_summary
# Define the hypotheses
H0 <- "The model with the predictors ln(x1), ln(x2), and ln(x3) is not useful in predicting ln(y) (all coefficients are zero)"
HA <- "At least one of the predictors ln(x1), ln(x2), or ln(x3) is useful in predicting ln(y) (at least one coefficient is not zero)"
# Print the hypotheses
cat("Null Hypothesis (H0):", H0, "\n")
cat("Alternative Hypothesis (HA):", HA, "\n")
# Extract the F-statistic and p-value
f_statistic <- model_summary$fstatistic[1]
p_value <- pf(f_statistic, model_summary$fstatistic[2], model_summary$fstatistic[3], lower.tail = FALSE)
# Print the results
cat("F-statistic:", f_statistic, "\n")
cat("p-value:", p_value, "\n")
# Check the significance at 0.05 level
if(p_value < 0.05) {
cat("The model is significant at the 0.05 level.\n")
} else {
cat("The model is not significant at the 0.05 level.\n")
}
---
output:
pdf_document: default
# Input the data
x1 <- c(0.93,1.11,0.93,1.11,0.93,1.11,0.93,1.11,1.02,1.02,1.02,1.02)
x2 <- c(1.00,1.00,1.00,1.00,1.40,1.40,1.40,1.40,1.18,1.18,1.18,1.18)
x3 <- c(0.20,0.20,0.50,0.50,0.20,0.20,0.50,0.50,0.31,0.31,0.31,0.31)
y <- c(32.95,38.72,35.20,38.72,32.27,39.71,33.67,38.72,35.20,33.67,36.02,32.27)
# Transform the variables using natural logarithms
ln_x1 <- log(x1)
ln_x2 <- log(x2)
ln_x3 <- log(x3)
ln_y <- log(y)
# Create a data frame
data <- data.frame(ln_x1, ln_x2, ln_x3, ln_y)
# Fit the linear regression model
model <- lm(ln_y ~ ln_x1 + ln_x2 + ln_x3, data = data)
# Display the summary of the model
model_summary <- summary(model)
model_summary
# Display the summary of the model
model_summary <- summary(model)
# Extract the F-statistic and p-value
f_statistic <- model_summary$fstatistic[1]
p_value <- pf(f_statistic, model_summary$fstatistic[2], model_summary$fstatistic[3], lower.tail = FALSE)
# Print the results
cat("F-statistic:", f_statistic, "\n")
cat("p-value:", p_value, "\n")
# Check the significance at 0.05 level
if(p_value < 0.05) {
cat("The model is significant at the 0.05 level.\n")
} else {
cat("The model is not significant at the 0.05 level.\n")
}
# Input the data
x1 <- c(0.93, 1.11, 0.93, 1.11, 0.93,1.11, 0.93,1.11,1.02,1.02,1.02,1.02)
x2 <- c(1,1,1,1,1.4,1.4,1.4,1.4,1.18,1.18,1.18,1.18)
x3 <- c(0.2,0.2,0.5,0.5,0.2,0.2, 0.5,0.5,0.31,0.31,0.31,0.31)
y <- c(32.95, 38.72, 35.2, 38.72, 32.27, 39.71, 33.67, 38.72, 35.2, 33.67, 36.02, 32.27)
# Transform the variables using natural logarithms
ln_x1 <- log(x1)
ln_x2 <- log(x2)
ln_x3 <- log(x3)
ln_y <- log(y)
# Create a data frame
data <- data.frame(ln_x1, ln_x2, ln_x3, ln_y)
# Fit the linear regression model
model <- lm(ln_y ~ ln_x1 + ln_x2 + ln_x3, data = data)
# Display the summary of the model
summary(model)
# Fit the linear regression model withoud ln(x2)
new_model <- lm(ln_y ~ ln_x1 + ln_x3, data = data)
# Display the summary of the new  model
summary(new_model)
# Fit the linear regression model withoud ln(x2), ln(x3)
new_model <- lm(ln_y ~ ln_x1, data = data)
# Display the summary of the new  model
summary(new_model)
stdError <- c(.03, .33, 1.69, .33, -.49, .96, .57, .33, -.25, -1.28, .29, -2.26)
plot(I(log(x1)), stdError)
plot(I(ln(x1)), stdError)
tinytex::install_tinytex()
---
title: "HW07_Lab_22125096"
```{r}
setwd("D:/STAT/Lab/Lab7")
getwd()
# Load the data
data <- read.csv("manuf.csv",header=TRUE)
# Log-transform the data
log_Y <- log(data$y)
log_K <- log(data$k)
log_L <- log(data$l)
log_E <- log(data$e)
log_M <- log(data$m)
model <- lm(log_Y~log_K+log_L+log_E+log_M)
knitr::opts_chunk$set(echo = TRUE)
summary(cars)
knitr::opts_chunk$set(echo = TRUE,warning = FALSE, message = FALSE)
model_reduce5c <- lm(log_y ~ log_l + log_m)
knitr::opts_chunk$set(echo = TRUE,warning = FALSE, message = FALSE)
library(car)
data5 <- read.csv("manuf.csv", header = TRUE)
str(data5)
log_y <- log(data5$y)
log_k <- log(data5$k)
log_l <- log(data5$l)
log_e <- log(data5$e)
log_m <- log(data5$m)
model5 <- lm(log_y ~ log_k + log_l + log_e + log_m)
summary(model5)
model_reduce5b <- lm(log_y ~ log_e + log_m)
anova(model_reduce5b, model5)
setwd('D:/STAT/Insurance')
getwd()
knitr::opts_chunk$set(echo = TRUE)
data <- read.csv("insurance.csv",header=TRUE)
str(data)
attach(data)
# Load necessary libraries
library(car)
library(caret)
# Load necessary libraries
install.packages("caret")
library(car)
library(caret)
library(dplyr)
# Load the dataset
data <- read.csv("insurance.csv")
knitr::opts_chunk$set(echo = TRUE,warning = FALSE, message = FALSE)
# Convert categorical variables to factors
data$sex <- as.factor(data$sex)
data$smoker <- as.factor(data$smoker)
data$region <- as.factor(data$region)
# Create dummy variables
data_encoded <- model.matrix(~.-1, data = data)
data_encoded <- as.data.frame(data_encoded)
data_encoded
# Calculate VIF
vif_data <- vif(lm(charges ~ ., data = data))
print(vif_data)
library(knitr)
knitr::opts_chunk$set(echo = TRUE,warning = FALSE, message = FALSE,tidy=TRUE)
# Fit a basic linear model
initial_model <- lm(charges ~ ., data = data)
# Check for normality of residuals using Shapiro-Wilk test
shapiro_test <- shapiro.test(residuals(initial_model))
print(shapiro_test)
# Draw boxplots for all columns to visualize outliers
par(mfrow=c(2,4))
for(i in 1:ncol(data)){
boxplot(data[,i], main=names(data)[i])
}
# Calculate Cook's distance
cooksD <- cooks.distance(initial_model)
# Identify influential observations
influential <- cooksD[(cooksD > (4/length(cooksD)))]
# Extract the indices of influential observations
outliers <- as.numeric(names(influential))
# Remove influential observations to clean the data
clean_data <- data[-outliers, ]
# Draw boxplots for all columns to visualize outliers after cleaning
par(mfrow=c(2,4))
for(i in 1:ncol(clean_data)){
boxplot(clean_data[,i], main=names(clean_data)[i])
}
# Percentage of data removed
cat("Percentage of data removed:", (dim(data)[1] - dim(clean_data)[1]) / dim(data)[1] * 100, "%\n")
# Fit a linear model on the cleaned data to check for normality again
clean_model <- lm(charges ~ ., data = clean_data)
shapiro_test_cleaned <- shapiro.test(residuals(clean_model))
print(shapiro_test_cleaned)
library(knitr)
knitr::opts_chunk$set(echo = TRUE,warning = FALSE, message = FALSE)
# Load necessary libraries
library(car)
library(caret)
library(dplyr)
library(MASS)
# Load the data set
data <- read.csv("insurance.csv")
# Load necessary libraries
library(car)
library(caret)
library(dplyr)
library(mass)
# Load necessary libraries
install.packages("mass")
library(car)
library(caret)
library(dplyr)
library(mass)
# Load necessary libraries
install.packages("mass")
library(car)
library(caret)
library(dplyr)
library(MASS)
# Load the data set
data <- read.csv("insurance.csv")
# Load necessary libraries
library(car)
library(caret)
library(dplyr)
library(MASS)
# Load the data set
data <- read.csv("insurance.csv")
# Convert categorical variables to factors
data$sex <- as.factor(data$sex)
data$smoker <- as.factor(data$smoker)
data$region <- as.factor(data$region)
# Create dummy variables
data_encoded <- model.matrix(~.-1, data = data)
data_encoded <- as.data.frame(data_encoded)
# Fit a basic linear model
initial_model <- lm(charges ~ ., data = data_encoded)
# Check for normality of residuals using Shapiro-Wilk test
shapiro_test <- shapiro.test(residuals(initial_model))
print(shapiro_test)
# Draw boxplots for all columns to visualize outliers
par(mfrow=c(2,4))
for(i in 1:ncol(data_encoded)){
boxplot(data_encoded[,i], main=names(data_encoded)[i])
}
# Calculate Cook's distance
cooksD <- cooks.distance(initial_model)
# Identify influential observations
influential <- cooksD[(cooksD > (4/length(cooksD)))]
# Extract the indices of influential observations
outliers <- as.numeric(names(influential))
# Remove influential observations to clean the data
clean_data <- data_encoded[-outliers, ]
# Draw boxplots for all columns to visualize outliers after cleaning
par(mfrow=c(2,4))
for(i in 1:ncol(clean_data)){
boxplot(clean_data[,i], main=names(clean_data)[i])
}
# Percentage of data removed
cat("Percentage of data removed:", (dim(data_encoded)[1] - dim(clean_data)[1]) / dim(data_encoded)[1] * 100, "%\n")
# Fit a linear model on the cleaned data to check for normality again
clean_model <- lm(charges ~ ., data = clean_data)
shapiro_test_cleaned <- shapiro.test(residuals(clean_model))
print(shapiro_test_cleaned)
# Calculate VIF
vif_data <- vif(lm(charges ~ ., data = clean_data))
# Calculate VIF
vif_data <- vif(lm(charges ~ ., data = data))
print(vif_data)
# Calculate VIF
vif_data <- vif(lm(charges ~ ., data = clean_data))
# Fit a basic linear model
initial_model <- lm(charges ~ ., data = data_encoded)
# Check for normality of residuals using Shapiro-Wilk test
shapiro_test <- shapiro.test(residuals(initial_model))
print(shapiro_test)
# Draw boxplots for all columns to visualize outliers
par(mfrow=c(2,4))
for(i in 1:ncol(data_encoded)){
boxplot(data_encoded[,i], main=names(data_encoded)[i])
}
# Calculate Cook's distance
cooksD <- cooks.distance(initial_model)
# Identify influential observations
influential <- cooksD[(cooksD > (4/length(cooksD)))]
# Extract the indices of influential observations
outliers <- as.numeric(names(influential))
# Remove influential observations to clean the data
clean_data <- data_encoded[-outliers, ]
# Draw boxplots for all columns to visualize outliers after cleaning
par(mfrow=c(2,4))
for(i in 1:ncol(clean_data)){
boxplot(clean_data[,i], main=names(clean_data)[i])
}
# Percentage of data removed
(dim(data_encoded)[1] - dim(clean_data)[1]) / dim(data_encoded)[1] * 100
# Fit a linear model on the cleaned data to check for normality again
clean_model <- lm(charges ~ ., data = clean_data)
shapiro_test_cleaned <- shapiro.test(residuals(clean_model))
print(shapiro_test_cleaned)
(dim(data_encoded)[1] - dim(clean_data)[1]) / dim(data_encoded)[1] * 100
clean_data
# Calculate VIF
vif_data <- vif(lm(charges ~ ., data = clean_data))
# Calculate VIF
vif_data <- vif(lm(charges ~ ., data = clean_data))
clean_data
# Define the full model
modFull <- lm(charges ~ ., data = clean_data)
# Define the null model
modZero <- lm(charges ~ 1, data = clean_data)
# Define an intermediate model (you can customize the variables as needed)
modInter <- lm(charges ~ age + bmi + children + sex + smoker + region, data = clean_data)
# Set seed for reproducibility
set.seed(123)
# Split data
train_index <- createDataPartition(clean_data$charges, p = 0.8, list = FALSE)
train_data <- clean_data[train_index, ]
test_data <- clean_data[-train_index, ]
# Set seed for reproducibility
set.seed(123)
# Split data
train_index <- createDataPartition(clean_data$charges, p = 0.8, list = FALSE)
train_data <- clean_data[train_index, ]
test_data <- clean_data[-train_index, ]
# Define the full model
modFull <- lm(charges ~ ., data = train_data)
# Define the null model
modZero <- lm(charges ~ 1, data = train_data)
# Define an intermediate model (you can customize the variables as needed)
modInter <- lm(charges ~ age + bmi + children + sex + smoker + region, data = train_data)
train_data
library(knitr)
knitr::opts_chunk$set(echo = TRUE,warning = FALSE, message = FALSE)
# Load necessary libraries
library(car)
library(caret)
library(dplyr)
library(MASS)
# Load the data set
data <- read.csv("insurance.csv")
# Encode the data
# Create pr_sex variable: 0 for female, 1 for male
data$pr_sex <- ifelse(data$sex == "male", 1, 0)
# Create pr_smoker variable: 0 for non-smoker, 1 for smoker
data$pr_smoker <- ifelse(data$smoker == "yes", 1, 0)
# Create dummy variables for region, excluding 'northwest' as the reference group
data$pr_region_northeast <- ifelse(data$region == "northeast", 1, 0)
data$pr_region_southeast <- ifelse(data$region == "southeast", 1, 0)
data$pr_region_southwest <- ifelse(data$region == "southwest", 1, 0)
## Handle data
```{r}
library(knitr)
knitr::opts_chunk$set(echo = TRUE,warning = FALSE, message = FALSE)
# Load necessary libraries
library(car)
library(caret)
library(dplyr)
library(MASS)
# Load the data set
data <- read.csv("insurance.csv")
# Encode the data
# Create pr_sex variable: 0 for female, 1 for male
data$pr_sex <- ifelse(data$sex == "male", 1, 0)
# Create pr_smoker variable: 0 for non-smoker, 1 for smoker
data$pr_smoker <- ifelse(data$smoker == "yes", 1, 0)
# Create dummy variables for region, excluding 'northwest' as the reference group
data$pr_region_northeast <- ifelse(data$region == "northeast", 1, 0)
data$pr_region_southeast <- ifelse(data$region == "southeast", 1, 0)
data$pr_region_southwest <- ifelse(data$region == "southwest", 1, 0)
# Fit the initial model
initial_model <- lm(charges ~ pr_sex + pr_smoker + pr_region_northeast + pr_region_southeast + pr_region_southwest + age + bmi + children, data = data)
# Check for normality of residuals using Shapiro-Wilk test
shapiro_test <- shapiro.test(residuals(initial_model))
print(shapiro_test)
# Draw boxplots for all columns to visualize outliers
par(mfrow=c(2,4))
for(i in 1:ncol(data)){
boxplot(data[,i], main=names(data)[i])
}
# Fit the initial model
initial_model <- lm(charges ~ pr_sex + pr_smoker + pr_region_northeast + pr_region_southeast + pr_region_southwest + age + bmi + children, data = data)
# Check for normality of residuals using Shapiro-Wilk test
shapiro_test <- shapiro.test(residuals(initial_model))
print(shapiro_test)
# Draw boxplots for all columns to visualize outliers
par(mfrow=c(2,4))
# Calculate Cook's distance
cooksd <- cooks.distance(initial_model)
# Identify influential points (top 10% by Cook's distance)
influential <- as.numeric(names(sort(cooksd, decreasing = TRUE)[1:floor(0.1 * length(cooksd))]))
# Remove influential observations
data_clean <- data[-influential, ]
# Check for NA values and remove them
if(any(is.na(data_clean))){
data_clean <- na.omit(data_clean)
}
# Draw boxplots for all columns to visualize outliers after cleaning
par(mfrow=c(2,4))
for(i in 1:ncol(data_clean)){
boxplot(data_clean[,i], main=names(data_clean)[i])
}
# Fit the initial model
initial_model <- lm(charges ~ pr_sex + pr_smoker + pr_region_northeast + pr_region_southeast + pr_region_southwest + age + bmi + children, data = data)
# Check for normality of residuals using Shapiro-Wilk test
shapiro_test <- shapiro.test(residuals(initial_model))
print(shapiro_test)
# Draw boxplots for all numeric columns to visualize outliers
numeric_columns <- sapply(data, is.numeric)
par(mfrow=c(2,4))
for(i in which(numeric_columns)){
boxplot(data[,i], main=names(data)[i])
}
# Calculate Cook's distance
cooksd <- cooks.distance(initial_model)
# Identify influential points (top 10% by Cook's distance)
influential <- as.numeric(names(sort(cooksd, decreasing = TRUE)[1:floor(0.1 * length(cooksd))]))
# Remove influential observations
data_clean <- data[-influential, ]
# Check for NA values and remove them
if(any(is.na(data_clean))){
data_clean <- na.omit(data_clean)
}
# Draw boxplots for all numeric columns to visualize outliers after cleaning
numeric_columns <- sapply(data_clean, is.numeric)
par(mfrow=c(2,4))
for(i in which(numeric_columns)){
boxplot(data_clean[,i], main=names(data_clean)[i])
}
# Percentage of data removed
(dim(data)[1] - dim(data_clean)[1]) / dim(data)[1] * 100
# Fit a linear model on the cleaned data to check for normality again
clean_model <- lm(charges ~ ., data = data_clean)
shapiro_test_cleaned <- shapiro.test(residuals(clean_model))
print(shapiro_test_cleaned)
# Calculate VIF
vif_data <- vif(lm(charges ~ ., data = data_clean))
data_clean
# Calculate VIF
vif_data <- vif(lm(charges ~.-data$sex,data$smoker,data$region, data = data_clean))
# Calculate VIF
vif_data <- vif(lm(charges ~ pr_sex + pr_smoker + pr_region_northeast + pr_region_southeast + pr_region_southwest + age + bmi + children, data = data_clean))
print(vif_data)
# Set seed for reproducibility
set.seed(123)
# Split data
train_index <- createDataPartition(data_clean$charges, p = 0.8, list = FALSE)
train_data <- data_clean[train_index, ]
test_data <- data_clean[-train_index, ]
# Set seed for reproducibility
set.seed(123)
# Split data
train_index <- createDataPartition(data_clean$charges, p = 0.8, list = FALSE)
train_data <- data_clean[train_index, ]
test_data <- data_clean[-train_index, ]
# Define the full model
modFull <- lm(charges ~ ., data = train_data)
# Define the null model
modZero <- lm(charges ~ 1, data = train_data)
# Define an intermediate model (you can customize the variables as needed)
modInter <- lm(charges ~ age + bmi + children + sex + smoker + region, data = train_data)
# Backward elimination with AIC
modBest1A <- stepAIC(modFull, direction = "backward", scope = list(lower = modZero, upper = modFull), k = 2)
# Backward elimination with BIC
modBest1B <- stepAIC(modFull, direction = "backward", scope = list(lower = modZero, upper = modFull), k = log(nrow(data)))
# Forward selection with BIC
modBest2 <- stepAIC(modZero, direction = "forward", scope = list(lower = modZero, upper = modFull), k = log(nrow(data)))
# Stepwise selection with BIC
modBest3 <- stepAIC(modInter, direction = "both", scope = list(lower = modZero, upper = modFull), k = log(nrow(data)))
# Define the full model
modFull <- lm(charges ~ pr_sex + pr_smoker + pr_region_northeast + pr_region_southeast + pr_region_southwest + age + bmi + children, data = train_data)
# Define the null model
modZero <- lm(charges ~ pr_sex + pr_smoker + pr_region_northeast + pr_region_southeast + pr_region_southwest + age + bmi + children, data = train_data)
# Define an intermediate model (you can customize the variables as needed)
modInter <- lm(charges ~ pr_sex + pr_smoker + pr_region_northeast + pr_region_southeast + pr_region_southwest + age + bmi + children, data = train_data)
# Backward elimination with AIC
modBest1A <- stepAIC(modFull, direction = "backward", scope = list(lower = modZero, upper = modFull), k = 2)
# Backward elimination with BIC
modBest1B <- stepAIC(modFull, direction = "backward", scope = list(lower = modZero, upper = modFull), k = log(nrow(data)))
# Forward selection with BIC
modBest2 <- stepAIC(modZero, direction = "forward", scope = list(lower = modZero, upper = modFull), k = log(nrow(data)))
# Stepwise selection with BIC
modBest3 <- stepAIC(modInter, direction = "both", scope = list(lower = modZero, upper = modFull), k = log(nrow(data)))
summary(modBest1A)
modBest1A <- stepAIC(modFull, direction = "backward", scope = list(lower = modZero, upper = modFull), k = 2)
# Define the full model
modFull <- lm(charges ~ pr_sex + pr_smoker + pr_region_northeast + pr_region_southeast + pr_region_southwest + age + bmi + children, data = train_data)
# Define the null model
modZero <- lm(charges ~ 1, data = train_data)
# Define an intermediate model (you can customize the variables as needed)
modInter <- lm(charges ~ pr_sex + pr_smoker + pr_region_northeast + age + bmi, data = train_data)
# Backward elimination with AIC
modBest1A <- stepAIC(modFull, direction = "backward", scope = list(lower = modZero, upper = modFull), k = 2)
# Backward elimination with BIC
modBest1B <- stepAIC(modFull, direction = "backward", scope = list(lower = modZero, upper = modFull), k = log(nrow(data)))
# Forward selection with BIC
modBest2 <- stepAIC(modZero, direction = "forward", scope = list(lower = modZero, upper = modFull), k = log(nrow(data)))
# Stepwise selection with BIC
modBest3 <- stepAIC(modInter, direction = "both", scope = list(lower = modZero, upper = modFull), k = log(nrow(data)))
